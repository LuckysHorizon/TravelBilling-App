# Frontend — Local Overrides (optional)
# Copy to .env.local if you need to override the monorepo root .env.development values.
# These take priority over the root env file.

VITE_API_BASE_URL=http://localhost:8080
VITE_PDF_SERVICE_URL=http://localhost:8000
